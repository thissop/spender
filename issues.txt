```
RuntimeError: User specified an unsupported autocast device_type 'mps'
```
- solution: make new miniconda environment (3.11), then execute the following command after activating it: `conda install pytorch torchvision torchaudio -c pytorch-nightly`. Then, execute the following in python in that environment to check if mps is available: 
```
>>> import torch
>>> print(torch.__version__, torch.backends.mps.is_available())
2.4.0.dev20240410 True
```

  File "/Users/thaddaeus/Documents/2. work/Research/GitHub/spender/spender/model.py", line 556, in _loss
    loss_ind = torch.sum(0.5 * w * (y - y_).pow(2), dim=1) / y.shape[1]
RuntimeError: The size of tensor a (4686) must match the size of tensor b (3921) at non-singleton dimension 1
(base) thaddaeus@Thaddaeuss-MacBook-Air train % 